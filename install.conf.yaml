- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.bashrc: vimrc
    ~/.gitconfig: gitconfig
    ~/.fonts: fonts

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [vim +PluginInstall +qall, Installing VIM plugins]im +PluginInstall +qall